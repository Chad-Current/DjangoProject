{% extends "dashboard/dashboard.html" %}
{% load static %}
{% block extra_css %}
    <link rel="stylesheet" href="{% static 'dashboard/css/listing.css' %}" type="text/css">
{% endblock %}
{% block title %}Important Documents{% endblock %}

{% block content %}
<div class="main">
    <div class="page-header">
        <div class="header-content">
            <h1 class="page-title">Important Documents</h1>
            <p class="page-subtitle">Manage your documents</p>
        </div>
        {% if can_modify %}
        <div class="header-action">
            <a href="{% url 'dashboard:importantdocument_create' %}" class="btn-add-contact">
                <i class="fas fa-plus-circle"></i>
                <span>Add Document</span>
            </a>
        </div>
        {% endif %}
    </div>

    <div class="contacts-content">
        {% if documents %}
        <div class="table-view d-none d-md-block">
            <div class="table-responsive">
                <table class="table contacts-table">
                    <thead>
                        <tr>
                            <th>Document Category</th>
                            <th>Title</th>
                            <th>Delegated to</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for document in documents %}
                        <tr>
                            <td>
                                <div class="contact-name">
                                    <i class="fas fa-file-alt contact-icon"></i>
                                    <span>{{ document.document_category }}</span>
                                </div>
                            </td>
                            <td><span class="contact-info">{{ document.name_or_title|truncatechars:50 }}</span></td>
                            <td><span>{{ document.delegated_important_document_to }}</span></td>
                            <td>
                                <div class="action-buttons">
                                    <a href="{% url 'dashboard:importantdocument_detail' document.pk %}" class="btn-action btn-edit" title="View document">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    {% if can_modify %}
                                    <a href="{% url 'dashboard:importantdocument_update' document.pk %}" class="btn-action btn-edit" title="Edit document">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a href="{% url 'dashboard:importantdocument_delete' document.pk %}" class="btn-action btn-delete" title="Delete document">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                    {% endif %}
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card-view d-md-none">
            {% for document in documents %}
            <div class="contact-card">
                <div class="card-header-section">
                    <div class="contact-avatar"><i class="fas fa-user-circle"></i></div>
                    <div class="contact-details">
                        <h3 class="contact-card-name">{{ document.name_or_title }}</h3>
                        <span class="badge-relationship-mobile">{{ document.document_category }}</span>
                    </div>
                </div>
                <div class="card-body-section">
                    <div class="info-label">Description</div>
                    <p class="contact-info-text">{{ document.description|truncatechars:50 }}</p>
                </div>
                {% if can_modify %}
                <div class="card-actions">
                    <a href="{% url 'dashboard:importantdocument_update' document.pk %}" class="btn-card-action btn-edit-card">
                        <i class="fas fa-edit"></i><span>Edit</span>
                    </a>
                    <a href="{% url 'dashboard:importantdocument_delete' document.pk %}" class="btn-card-action btn-delete-card">
                        <i class="fas fa-trash-alt"></i><span>Delete</span>
                    </a>
                </div>
                {% endif %}
            </div>
            {% endfor %}
        </div>

        {% else %}
        <div class="empty-state">
            <div class="empty-state-icon"><i class="fas fa-address-book"></i></div>
            <h2 class="empty-state-title">No Documents Yet</h2>
            <p class="empty-state-text">Start building your documents list.</p>
            {% if can_modify %}
            <a href="{% url 'dashboard:importantdocument_create' %}" class="btn-add-first">
                <i class="fas fa-plus-circle"></i> Add Your First Document
            </a>
            {% endif %}
        </div>
        {% endif %}
    </div>
</div>
{% endblock %}