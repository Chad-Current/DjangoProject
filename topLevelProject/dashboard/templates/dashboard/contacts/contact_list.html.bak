{% extends "dashboard/dashboard.html" %}
{% load static %}
{% block extra_css %}
    <link rel="stylesheet" href="{% static 'dashboard/css/listing.css' %}" type="text/css">
{% endblock %}
{% block title %}Contacts{% endblock %}

{% block content %}
<div class="main">
    <div class="page-header">
        <div class="header-content">
            <h1 class="page-title">Contacts</h1>
            <p class="page-subtitle">Manage your contact information</p>
        </div>
        {% if can_modify %}
        <div class="header-action">
            <a href="{% url 'dashboard:contact_create' %}" class="btn-add-contact">
                <i class="fas fa-plus-circle"></i>
                <span>Add Contact</span>
            </a>
        </div>
        {% endif %}
    </div>

    <div class="contacts-content">
        {% if contacts %}
        <div class="table-view d-none d-md-block">
            <div class="table-responsive">
                <table class="table contacts-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Relationship</th>
                            <th>Emergency Contact</th>
                            <th>Phone</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for contact in contacts %}
                        <tr>
                            <td>
                                <div class="contact-name">
                                    <i class="fas fa-address-book contact-icon"></i>
                                    <span>{{ contact.contact_name }}</span>
                                </div>
                            </td>
                            <td><span>{{ contact.contact_relation }}</span></td>
                            <td><span class="contact-info">{{ contact.is_emergency_contact|yesno:"Yes,No" }}</span></td>
                            <td><span class="contact-info">{{ contact.phone|phone2numeric }}</span></td>
                            <td>
                                <div class="action-buttons">
                                    <a href="{% url 'dashboard:contact_detail' contact.pk %}" class="btn-action btn-view" title="View contact">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    {% if can_modify %}
                                    <a href="{% url 'dashboard:contact_update' contact.pk %}" class="btn-action btn-edit" title="Edit contact">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a href="{% url 'dashboard:contact_delete' contact.pk %}" class="btn-action btn-delete" title="Delete contact">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                    {% endif %}
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card-view d-md-none">
            {% for contact in contacts %}
            <div class="contact-card">
                <div class="card-header-section">
                    <div class="contact-avatar"><i class="fas fa-user-circle"></i></div>
                    <div class="contact-details">
                        <h3 class="contact-card-name">{{ contact.contact_name }}</h3>
                        <span class="badge-relationship-mobile">{{ contact.contact_relation }}</span>
                    </div>
                </div>
                <div class="card-body-section">
                    <div class="info-label">Phone</div>
                    <p class="contact-info-text">{{ contact.phone|phone2numeric }}</p>
                </div>
                {% if can_modify %}
                <div class="card-actions">
                    <a href="{% url 'dashboard:contact_update' contact.pk %}" class="btn-card-action btn-edit-card">
                        <i class="fas fa-edit"></i><span>Edit</span>
                    </a>
                    <a href="{% url 'dashboard:contact_delete' contact.pk %}" class="btn-card-action btn-delete-card">
                        <i class="fas fa-trash-alt"></i><span>Delete</span>
                    </a>
                </div>
                {% endif %}
            </div>
            {% endfor %}
        </div>

        {% else %}
        <div class="empty-state">
            <div class="empty-state-icon"><i class="fas fa-address-book"></i></div>
            <h2 class="empty-state-title">No Contacts Yet</h2>
            <p class="empty-state-text">Start building your contact list by adding your first contact.</p>
            {% if can_modify %}
            <a href="{% url 'dashboard:contact_create' %}" class="btn-add-first">
                <i class="fas fa-plus-circle"></i> Add Your First Contact
            </a>
            {% endif %}
        </div>
        {% endif %}
    </div>
</div>
{% endblock %}